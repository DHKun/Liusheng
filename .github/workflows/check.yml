name: Quality and UI regression

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    uses: ./.github/workflows/quality.yml
  appimage-package:
    name: Linux AppImage
    uses: ./.github/workflows/package-appimage.yml
