# 0.3.0 升级工作记录

基线：fed28d5 / 0.2.1。版本升级为 0.3.0。现有 `routing-demo/` 保持原样，宿主机已安装程序保持原样。

已完成：缓存曲库先显示；后台按路径增量更新；扫描失败和离线保护；异步搜索；正式 Qt 列表模型；按需封面、提色和版本清理；异步预加载；SPSC 输出缓冲；取消与超时；循环/随机及队列排序；歌单和 M3U8；暂停会话恢复；目录/设备设置；文件打开及单实例；歌词偏移、信号链；构建检查与文档。

验证：93 项 workspace/音频合约测试、严格 Clippy、rustfmt、debug/release 离屏页面和功能测试、单实例/MPRIS、隔离虚拟 PipeWire 播放、安装脚本临时 DESTDIR 测试均通过。release 构建约 19 MB。1,000/10,000 首合成缓存曲库各执行 7 次启动测量。

原生 macOS、Fedora/KDE/Wayland 交互与 N9/DAC 的硬件格式、热插拔及听感属于实机验收范围。详细实现、指标定义、复现命令和边界见 `UPGRADE_0_3.md`。
